# TITAN / JARVIS Claude Compatibility

Follow [AGENTS.md](AGENTS.md) as the canonical repository operating contract.

Use the same inspect → plan → implement → validate → diff-review workflow. Preserve existing architecture, keep secrets out of source control, use documented integration boundaries, and never fake external service health.
